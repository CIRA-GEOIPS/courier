apiVersion: geoips/driver/v1
kind: Service
name: testing_spec_not_complete
docstring: |
description: This is for testing! Not a complete spec.
spec:
  service_namespace: example_service1
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: admin
    password: admin_test
  run:
    - file_watching_service:
        kind: data_monitor
        name: FileSystemPoller-Watchdog # this is the plugin name
        config:
          path: /workspaces/geoips_driver/tests/fake-watch-dir
    - this_builds_jobs:
        kind: job_builder
        name: DummyJobBuilder
        config: null # just using defaults
    - dispatch_those_jobs:
        kind: dispatcher
        name: serial_bash_dispatcher
        config:
          bash_script: |
           echo {file}
